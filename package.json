{
  "name": "my-app",
  "version": "1.0.0",
  "type": "module",
  "dependencies": {
    "cookie-parser": "^1.4.7",
    "dotenv": "^17.2.3",
    "ejs": "^3.1.10",
    "express": "^4.22.1",
    "mysql2": "^3.15.3",
    "nodemon": "^3.1.11",
    "uuid": "^13.0.0"
  },
  "scripts": {
    "dev": "nodemon server.js",
    "wbuild": "rmdir /S /Q dist & mkdir dist & xcopy server.js dist /Y & xcopy public dist\\public /E /I & xcopy view dist\\view /E /I & xcopy webpage dist\\webpage /E /I",
    "build": "rm -rf dist && mkdir dist && cp server.js dist/ && cp -r public dist/ && cp -r view dist/ && cp -r webpage dist/"
  }
}
